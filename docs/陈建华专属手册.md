# 陈建华专属总手册（所有项目通用交接单 · 新开云端/新对话第一必读）

> ⚠️ 陈先生英文大笨蛋版本：全文中文，按顺序读，照着做就行。
> 
> **本文档保存位置（3处三重备份，永远不会丢）**：
> 1. 云端工作盘 `/workspace/陈建华专属手册.md`（TRAE 同一工作区内任意新对话都能读到）
> 2. GitHub 私有仓 `gogotb81849/chen-handbook` 根目录（独立私人手册仓库，就算 TRAE 云端工作区重置了，只要登 GitHub 照样能拿回来）
> 3. 各项目仓库内 `docs/` 子目录：
>    - 熊猫笔记：`gogotb81849/panda-note/docs/陈建华专属手册.md`
>    - 海上蔬菜：`gogotb81849/haishang-shucai-zhongzhi/docs/陈建华专属手册.md`

---

## 零、★ 新开一个云端实例 / 新开一个对话时（发下面第一句话就行，复制粘贴）

```
你好！请先阅读这个文件：/workspace/陈建华专属手册.md
按手册里的"一、5条工作铁规矩"和"二、GitHub凭据恢复步骤"执行，再继续我们的对话。
```

> 如果 `/workspace` 整个目录都没了（整个云端工作区重置），请先让我：
> "用 GitHub 账号 gogotb81849 把私有仓库 chen-handbook 克隆到 /workspace/chen-handbook，所有信息在里面。"
> （GitHub Token 见本文档第 二 节，Token 只允许陈先生告诉一次，用完一定要按第二节第3步写回 /workspace/.git-credentials）

---

## 一、5条工作铁规矩（任何项目共用，AI 助手每次必须遵守）

1. **先读后改**：每次修改前先读对应项目的规格文档 / 001优化前提文件，不许上来就改代码。
2. **改完立刻提交并部署**（001文档强约束）：
   ```
   git status → add → commit（写清"为什么改"）→ 打 TAG X.Y.Z.MMDD
   → git push origin main → git push origin <TAG>
   ```
3. **凭据/Token/密码只问陈先生一次**：收到立刻持久化保存到指定位置（见下文"六、凭据持久化位置地图"），下下次自动用，不许再问第二次。
4. **严禁破坏数据**：严禁 `prisma db push --force-reset`、严禁 `DROP TABLE/TRUNCATE`、严禁删除已有字段（只能加字段）。
5. **回复必须称呼"陈先生"**。修改/部署结果要明确说：已推到 GitHub？版本号？部署 Run 在哪？用户要做什么（Ctrl+F5 等）？

---

## 二、GitHub 账号与 Token（最重要！）

| 项目 | 值 |
|---|---|
| **GitHub 用户名** | `gogotb81849` |
| **GitHub 登录邮箱** | （陈先生自己的邮箱，不用记在这里） |
| **GitHub PAT（个人访问令牌）** | ⚠️ 【严禁写在文档里】请在 GitHub 个人设置 → Developer settings → Personal access tokens 查看/创建。权限要求见下一行。Token 只存到 ① 本地 `.git-credentials` 文件（chmod 600）② GitHub Secrets 中，绝不要写到 md/yaml/json 代码文件里。 |
| **Token 权限要求** | `repo`（仓库读写）+ `workflow`（Actions）+ `read:org`（可选，gh CLI 用） |
| **Token 过期时间** | （陈先生查 GitHub 个人设置 → Developer settings → Personal access tokens，如果过期换新后，立刻更新写回 git-credentials） |

### 1. 云端实例凭证恢复命令（只要云端重置/新建就跑一次）
```bash
# ============================================================
# ⚠️ 重要：执行本脚本前，先把下面两行里的 YOUR_GITHUB_PAT_HERE
#        替换成陈先生自己在 GitHub 上实际持有的 PAT（ghp_ 开头）
#        （不要把真实 PAT 写回到本文档的示例里，会被 GitHub 推送保护拦截）
# ============================================================
MY_PAT="YOUR_GITHUB_PAT_HERE"

# 写入全局 git-credentials（推荐：放 /workspace/.git-credentials 不会丢，放 /root 会丢）
mkdir -p /root
cat > /workspace/.git-credentials <<EOF
https://gogotb81849:${MY_PAT}@github.com
EOF
chmod 600 /workspace/.git-credentials
unset MY_PAT

# Git 全局配置（所有项目默认用这个 store 文件，避免 /root 丢失）
git config --global credential.helper 'store --file=/workspace/.git-credentials'
git config --global user.name '陈建华（gogotb81849）'
git config --global user.email 'gogotb81849@users.noreply.github.com'
git config --global safe.directory '*'

# gh CLI 登录（可选，用来创建新仓库）—— 不要把 Token 明文 echo，改用变量
# (前提：上面的 MY_PAT 变量还在当前 shell 里没 unset 时，或者重新赋值一次)
# MY_PAT="YOUR_GITHUB_PAT_HERE"; echo "$MY_PAT" | gh auth login --with-token --hostname github.com >/dev/null 2>&1 || true
# unset MY_PAT

# 验证（不需要密码就能 git push）
git ls-remote https://github.com/gogotb81849/panda-note.git HEAD >/dev/null && echo "✅ GitHub 凭据已恢复"
```

### 2. 陈先生创建新仓库的格式（小写 + 连字符）
- 不能有中文、空格、大写字母；单词之间用 `-`
- 例子：`haishang-shucai-zhongzhi` / `panda-note` / `chen-handbook`

---

## 三、★ 所有项目总清单（陈先生目前的 3 个仓库）

| # | 项目名称 | 本地目录 | GitHub 私有仓库 URL | 项目说明 | 最新 TAG |
|---|---|---|---|---|---|
| 1 | 🐼 熊猫笔记（船舶政工智慧管理） | `/workspace/panda-note` | https://github.com/gogotb81849/panda-note | 工作日志、日历/日程、船舶业务（核心生产项目） | 1.1.0.1226 |
| 2 | 🌱 海上蔬菜种植 | `/workspace/haishang-shucai-zhongzhi` | https://github.com/gogotb81849/haishang-shucai-zhongzhi | 船舶密闭舱室蔬菜智能规划可视化大屏；规格书已写好，待生成完整工程 | 1.0.0.0812 |
| 3 | 📘 陈建华专属手册（本手册） | `/workspace/chen-handbook`（可 git clone） | https://github.com/gogotb81849/chen-handbook | **纯私人仓库，保存所有密码/凭据/规格书/操作步骤** | 1.0.0.0812 |

> 克隆项目的标准命令（任何云端实例 3 条搞定）：
> ```bash
> cd /workspace
> git clone https://github.com/gogotb81849/panda-note.git
> git clone https://github.com/gogotb81849/haishang-shucai-zhongzhi.git
> git clone https://github.com/gogotb81849/chen-handbook.git
> ```

---

## 四、🐼 熊猫笔记专属资料（服务器、账号、规格书）

### 4.1 规格文档 & 部署规则（必看）
- 总规则文档：`/workspace/panda-note/001优化前提文件.md`（任何改动前必须读）
- 本手册也有：`/workspace/陈建华专属手册.md`（本文件）
- 服务器完整密码本：`/workspace/panda-note/docs/服务器配置与账号信息.md`

### 4.2 生产服务器总览
| 项目 | 值 |
|---|---|
| **服务器公网 IP** | **106.14.57.62** |
| **宝塔面板地址** | http://106.14.57.62:8888 |
| **宝塔账号** | `gogotb` |
| **宝塔密码** | `BTroot260318` |
| **SSH（root）** | `ssh root@106.14.57.62 -p 22`，密码同 `BTroot260318`（也可配密钥，见 docs/服务器配置与账号信息.md 第四节） |
| **前端地址** | http://106.14.57.62:3000 （生产） |
| **后端端口** | 3002（健康检查：`curl http://106.14.57.62:3002/api/health`） |
| **进程管理** | PM2：`pm2 status` / `pm2 logs nav-log-backend --lines 50` / `pm2 restart nav-log-backend` |

### 4.3 数据库（PostgreSQL 15）
| 项目 | 值 |
|---|---|
| 数据库名 | `navlog` |
| 用户名 | `navlog` |
| 密码 | `navlog123` |
| 端口 | 5432 |
| **完整 .env DATABASE_URL** | `postgresql://navlog:navlog123@127.0.0.1:5432/navlog?schema=public` |

### 4.4 对象存储 MinIO
| 项目 | 值 |
|---|---|
| Access Key | `navlog` |
| Secret Key | `navlog123` |
| Bucket | `navlog-files` |

### 4.5 应用测试账号（前端登录 http://106.14.57.62:3000 ）
| 用户名 | 密码 | 角色 |
|---|---|---|
| `gogotb` | `123456` | admin（系统管理员，多角色） |
| `陈建华` | `123456` | shore_crew_supervisor（船工主管） |
| `supervisor` | `123456` | ship_political_instructor（船舶政委） |

### 4.6 部署方式
**自动部署**：`git push origin main` 推送到 panda-note 仓库后，GitHub Actions 会自动构建并 SSH 部署到 106.14.57.62。
- Actions 监控地址：https://github.com/gogotb81849/panda-note/actions
- Actions Secrets 已经在 GitHub 仓库配置好（SSH_PRIVATE_KEY / DEPLOY_HOST=106.14.57.62 / DEPLOY_USER=root / DEPLOY_PATH 等）。

---

## 五、🌱 海上蔬菜专属资料（项目 2）

### 5.1 TRAE 规格书（完整 10 章）
- 位置：`/workspace/haishang-shucai-zhongzhi/ship_plant_bigscreen_spec.md`
- 核心硬约束：船舶**完全密闭无自然光，LED 24h 恒光**，剔除所有日出日落、昼夜、光照强度逻辑。
- 技术栈：Vue3+TS+Vite+ECharts+Three.js 前端；SpringBoot 2.7 + JDK17 + MyBatis-Plus + MySQL 8 后端；WebSocket 实时推送 + POI Excel 导出。
- 9 张 SQL 建表、7 条数学公式、24 栅格大屏布局、分层业务模块全部在规格书中写死。

### 5.2 待生成的完整工程结构
```
ship-plant-big-screen/
├── frontend/   (Vue3大屏)
├── backend/    (SpringBoot后端)
├── docs/       (规格书/接口文档)
└── README.md
```
（规格书第 九 节有 15 步代码生成执行顺序，照着生成即可。）

---

## 六、所有凭据持久化位置地图（防止云端重置）

| 凭据类型 | 本地保存路径（首选：/workspace 下，最持久） | 备份（GitHub 私有） |
|---|---|---|
| **GitHub PAT** | `/workspace/.git-credentials`（权限 600），格式：`https://gogotb81849:<TOKEN>@github.com`；Git 全局 credential.helper 指向它 | `chen-handbook` 本手册 第二节 |
| **全局 Git 配置** | `~/.gitconfig`（每次云端重置后按第二节命令重建） | 第二节命令 |
| **SSH 私钥（部署用）** | `~/.ssh/panda-nav-server`（权限 600），如有。如无，按 docs/服务器配置与账号信息.md 第四节的 ssh-keygen 步骤重新生成并部署公钥到 106.14.57.62 | `chen-handbook` 里记录回退 SSH 密码 `BTroot260318` |
| **宝塔/SSH/数据库/MinIO 全部密码** | `/workspace/panda-note/docs/服务器配置与账号信息.md` | `chen-handbook/服务器配置与账号信息.md` + `panda-note/docs/` + `haishang-shucai-zhongzhi/docs/` |
| **规格文档与操作手册** | `/workspace/陈建华专属手册.md`（本文件） | 3 个 GitHub 仓库各一份（见文档顶部列表） |

---

## 七、开新对话框 / 新云端实例后的 60 秒快速恢复检查单

把下面这段直接丢给新对话里的 AI 助手，**复制粘贴不用改**：

```
【新对话启动 Checklist · 按顺序执行】
1. 读 /workspace/陈建华专属手册.md 全文
2. 如果 /workspace/.git-credentials 不存在 → 按手册第二节 1. 小节运行"云端实例凭据恢复命令"
3. 检查三个项目目录是否都在：
   ls /workspace/panda-note /workspace/haishang-shucai-zhongzhi
   如果缺失 → 按手册第三节底部 3 条 git clone 命令克隆
4. 验证 GitHub 凭据：
   cd /workspace/panda-note && git ls-remote origin HEAD
5. 回答："陈先生，手册已读，凭据已恢复，3 项目就绪，等您指示。"
```

---

> 文档版本号：1.0.0.0812（2026-08-12 创建）
> 下次更新这个手册：任何新账号、新服务器、新仓库、新密码创建后，立刻来追加一行；修改后必须 commit + push 到 3 个 GitHub 仓库（chen-handbook + panda-note/docs + haishang-shucai-zhongzhi/docs）。
