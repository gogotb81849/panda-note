# // ====== v10-v20 旧版配置（已全部注释掉，运行成功后删除）====================================
# # 【CI 安全·v0816-20】与 frontend/.npmrc 同步（只放 old-space，gc-interval 放 NODE_OPTIONS）
# node-options=--max-old-space-size=8192 --max-semi-space-size=8 --expose-gc
# fetch-timeout=600000
# // ====== 以上全部注释掉 ========================================================================

# ★ v21 干净版：与 frontend/.npmrc 同步，只留 --max-old-space-size=6144
node-options=--max-old-space-size=6144
fetch-timeout=600000
