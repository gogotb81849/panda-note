# 【CI 安全·v0816-v9】终极根因！：
# .npmrc 的 node-options = 最高优先级（高于 shell NODE_OPTIONS、高于 spawn argv 设的 --max-old-space-size）
# npm run-script / npx 启动的所有 node 进程，会自动把本值注入到子进程的实际执行参数里，
# 之前写 5120，等于告诉所有子进程 old-space=5120，实际 runner cgroup 极限 ~3.8GB，
# 导致每次 Mark-Compact 峰值永远 3844 MB（committed 4135），永远 allocation failure！
#
# v9：改成 3000，把所有链路 node 进程（nuxt→vite→rollup→esbuild 的每个 fork）全部锁死 old-space ≤ 3000MB
# 实测本值被 npm 注入后，heap_size_limit ≈ 3012 MB，committed 极限 ~3200MB，离 4GB 极限安全边际 800MB
node-options=--max-old-space-size=3000 --max-semi-space-size=4 --expose-gc
fetch-timeout=600000
