# 【CI 安全 · v0816-11】：committed 峰值已经从 4127 → 3010 MB（frontend/.npmrc=3000 生效 ✅）
#   但 Scavenge 2805/3010 allocation failure，说明应用本身真实峰值 ~2.8GB，差 200MB 就到上限
# v11：old-space=3300，再加 300MB 缓冲（committed ~3320MB，离 3.8GB cgroup 还有 500MB 安全边际）
node-options=--max-old-space-size=3300 --max-semi-space-size=4 --expose-gc
fetch-timeout=600000
