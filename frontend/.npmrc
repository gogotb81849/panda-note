# 【CI 安全 · v0816-12】：v11 GC trace = Mark-Compact 3058.7/3307.7（old-space=3300 用了 3058，OOM 在边界）
# v12：再 +300MB 缓冲，old-space=3600 → committed 预期峰值 ~3610 < 3800 cgroup（留 200MB 安全边际）
node-options=--max-old-space-size=3600 --max-semi-space-size=4 --expose-gc
fetch-timeout=600000
