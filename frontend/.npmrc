# 【CI 安全 · v0816-19】：18 轮证明 used 永远紧跟 old-space（V8 GC 太懒！old-space 越大 used 越高）
#   v16(4096) used=3820 | v18(8192) used=7662 → used 翻倍！不是应用需要这么多，是 V8 推迟 GC 导致垃圾堆积
#   v19 修复：--gc-interval=100 强制每 100 个 old-space 页面 GC 一次（约每 50MB 一次）
#   预期 used 从 7662 降到 ~4000（因为 GC 更频繁，垃圾来不及堆积）
node-options=--max-old-space-size=8192 --max-semi-space-size=8 --expose-gc --gc-interval=100 --gc-global
fetch-timeout=600000
