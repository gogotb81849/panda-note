# 【CI 安全 · 再次加固 v0814b】GitHub Actions 7GB runner 的 cgroup 极限约 6.8GB，
# max-old-space-size=5120 历史记录多次 OOM，RSS 峰值 ~6.8G SIGKILL 后 GitHub Actions step 静默成功（坑）
# 本次降到 4096 = 4GB。结合 vite.build.minify=esbuild + maxParallelFileOps=2，
# 预期 RSS ≈ 4G(V8 堆) + ~500M(non-heap/esbuild externals) ≈ 4.5G，安全通过 7GB runner。
node-options=--max-old-space-size=4096 --max-semi-space-size=16 --expose-gc
fetch-timeout=600000
