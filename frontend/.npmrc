# 【CI 安全 · v0814c 终极压内存】GitHub Actions 7GB runner cgroup≈6.8G 极限。
# Run #149 真红了（感谢 ci-build-frontend.mjs），exit 1 证明 4096 还是 OOM。
# 本轮降到 3072 = 3GB（V8 堆），配合 nuxt.config.ts CI 时禁用 PWA/workbox、
# maxParallelFileOps=1、optimizeDeps.exclude echarts/element-plus，
# 预期 RSS ≈ 3G + ~400M ≈ 3.4G，7GB runner 稳稳过。
node-options=--max-old-space-size=3072 --max-semi-space-size=16 --expose-gc
fetch-timeout=600000
