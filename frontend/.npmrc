# 【CI 安全 · v0816-10 ★ 终极修复】：working-directory=frontend 时，npm 优先读 frontend/.npmrc
# 本文件的 node-options 才是真正会被 Step9 npm run build 使用的值！
# 之前写 4096 → V8 old-space=4096，但 runner cgroup 极限 ~3.8GB，
#   于是 Mark-Compact 永远在 3828MB（committed 4127MB）处 allocation failure。
# v10：old-space=3000，安全边际 ~800MB；再配合 treeshake smallest / sourcemap=false / 1s GC interval
node-options=--max-old-space-size=3000 --max-semi-space-size=4 --expose-gc
fetch-timeout=600000
